# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md
#
pre-commit:
    parallel: true
    commands:
        format:
            run: bun run format
        markdown-lint:
            glob: "*.md"
            run: bunx alex .
        biome-lint:
            glob: "*.{js,ts}"
            run: bunx biome line --write {staged_files}
